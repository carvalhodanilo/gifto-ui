/**
 * Cores padrão do tema quando o tenant vem apenas de TenantOption (id, fantasyName).
 * Usado para construir Tenant no admin-app ao selecionar no login.
 */
export const DEFAULT_PRIMARY_COLOR = '#0f172a';
export const DEFAULT_SECONDARY_COLOR = '#64748b';
